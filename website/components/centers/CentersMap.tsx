export function CentersMap() {
  return (
    <div className="h-64 rounded-xl overflow-hidden bg-brand-50 flex items-center justify-center border border-brand-100">
      <p className="text-sm text-brand-400 font-medium">
        [Map embed — configure NEXT_PUBLIC_MAPS_API_KEY in .env]
      </p>
    </div>
  );
}
